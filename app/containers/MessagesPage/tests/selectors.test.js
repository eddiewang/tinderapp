import {
  selectMessagesPage,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectMessagesPage();

describe('selectMessagesPage', () => {

});
